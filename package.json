{
  "name": "langfuse-prompt-transfer",
  "version": "1.0.0",
  "description": "Transfer prompts between Langfuse instances (e.g., from US Cloud to private self-hosted)",
  "main": "dist/transfer-prompts.js",
  "types": "dist/transfer-prompts.d.ts",
  "scripts": {
    "start": "npx ts-node transfer-prompts.ts",
    "dry-run": "npx ts-node transfer-prompts.ts --dry-run",
    "transfer-all": "npx ts-node transfer-prompts.ts --all-versions",
    "export": "npx ts-node transfer-prompts.ts export",
    "import": "npx ts-node transfer-prompts.ts import",
    "build": "tsc",
    "build:run": "tsc && node dist/transfer-prompts.js"
  },
  "keywords": [
    "langfuse",
    "prompt",
    "transfer",
    "migration",
    "llm",
    "ai"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0"
  },
  "repository": {
    "type": "git",
    "url": ""
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}